server:
  port: 8085
  servlet:
    context-path: /

spring:
  application:
    name: ryl-miniprogram-server
  profiles:
    active: dev
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: jdbc:sqlserver://IRYLSERVER:1433;databaseName=ryl_engineer;encrypt=false
    username: sa
    password: lfkj2025@
    hikari:
      minimum-idle: 5
      maximum-pool-size: 15
      auto-commit: true
      idle-timeout: 30000
      pool-name: HikariCP
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-test-query: SELECT 1
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 15MB
      
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.ryl.miniprogram.entity
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
      
pagehelper:
  helper-dialect: sqlserver
  reasonable: true
  support-methods-arguments: true
  params: count=countSql

# 微信小程序配置
wx:
  miniapp:
    appid: ${WX_MINIAPP_APPID:wx6bb03fe65c98cfd0}
    secret: ${WX_MINIAPP_SECRET:8579cf94da60c6892a61c918e4624f4f}
    token: ${WX_MINIAPP_TOKEN:your_miniapp_token}
    aesKey: ${WX_MINIAPP_AES_KEY:your_miniapp_aes_key}
    msgDataFormat: JSON
    template-ids:
      engineer-assigned: 'YOUR_TEMPLATE_ID_HERE'
      quote-generated: 'YOUR_TEMPLATE_ID_HERE'
      price-confirmed: 'YOUR_TEMPLATE_ID_HERE'
      service-completed: 'YOUR_TEMPLATE_ID_HERE'
      evaluation-received: 'YOUR_TEMPLATE_ID_HERE'
    page:
      task-detail: 'pages/task/taskdetail'

# JWT配置
jwt:
  secret: ${JWT_SECRET:ryl_miniprogram_jwt_secret_key}
  expiration: ${JWT_EXPIRATION:86400000}
  header: Authorization
  tokenHead: Bearer

logging:
  level:
    com.ryl.miniprogram: debug

# 文件上传配置
file:
  upload:
    # path: uploads # 上传文件存储路径
  allowed-file-types: ${FILE_ALLOWED_TYPES:.jpg,.jpeg,.png,.gif,.pdf,.doc,.docx,.xls,.xlsx,.ppt,.pptx}
  max-file-size: ${FILE_MAX_SIZE:10485760} 